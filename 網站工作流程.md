# 祐興食品網站工作流程 (2026-05-07)

## 📋 問題清單

### 1. 新增12款產品時位置錯亂
- 插入點唔精準，導致卡片出現在錯誤位置

### 2. HTML Div結構破壞
- Div平衡被破壞，導致格式問題

### 3. 「素纖麵」錯字問題
- Unicode byte sequence混淆

### 4. 無驗證機制
- 依賴Ben發現問題，而非主動驗證

### 5. 無文件化流程
- 每次「跟住感覺做」

---

## 💡 解決方案

### 標準工作流程

**每次改動網站前：**
1. 📦 建立backup (copy to 備份資料夾)
2. 📝 寫低今次要做乜 (task list)
3. 🔍 確認目標狀態

**改動時：**
4. ✏️ 只做一樣嘢
5. 👁️ 改完開browser快速檢查
6. ✅ 跑div balance script確認無爛

**完成後：**
7. 💾 Commit + push
8. 📢 通知Ben

### 技術檢查清單

每次commit前必須通過：
- [ ] `<div>` 數量 == `</div>` 數量
- [ ] 目標byte sequence正確
- [ ] Browser显示正确

---

## 🔧 常用Debug Script

### 檢查div平衡
```python
data = open('index.html', 'rb').read()
print('<div>:', data.count(b'<div'))
print('</div>:', data.count(b'</div>'))
```

### 檢查正確byte sequence
```python
# 請Ben提供正確text示例
correct_text = "素纖麵"
correct_bytes = correct_text.encode('utf-8').hex()
print('正確bytes:', correct_bytes)
```